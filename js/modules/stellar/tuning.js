/**
 * @author Kate Compton
 */
define([], function() {
    var tuning = {
        lod : {
            inspectFadeLow : 0,
            inspectFadeHigh : .2,
            triggerInspectMode : .1,
            triggerUniverseMode : .8,
            inspectorStart : .05,
        }
    };
    return tuning;
});
